# ساخت اپ اندروید گام

این شاخه android-app برای تبدیل نسخه فعلی گام به اپ اندروید با Capacitor آماده شده است.

## کاری که آماده شده

- رابط فعلی سایت بدون بازنویسی از صفر وارد اپ می‌شود.
- LocalStorage فعلی حفظ می‌شود؛ بنابراین اطلاعات و تیک‌ها روی همان دستگاه باقی می‌مانند.
- نسخه وب/PWA فعلی هم حفظ می‌شود.
- شناسه اپ: com.mynafshe.gam
- نام اپ: گام

## راه‌اندازی روی ویندوز

در پوشه پروژه این دستورها را اجرا کن:

npm install
npx cap add android
npx cap sync android
npx cap open android

بعد از ساخته‌شدن پوشه android، پروژه را در Android Studio باز کن و APK را Build کن.

## بعد از هر تغییر در سایت

npx cap sync android

## وضعیت Sync بین گوشی و PC

نسخه فعلی هنوز Sync ابری کامل ندارد. داده‌های فعلی با LocalStorage روی هر دستگاه جداگانه ذخیره می‌شوند. مرحله بعدی Sync واقعی باید با یک backend مشترک انجام شود.

## نکته

پوشه android عمداً در این commit ساخته نشده، چون باید توسط خود Capacitor و محیط Android تولید شود.
